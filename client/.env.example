# Base URL of the Express API (Render). Read at BUILD time by next.config.ts
# rewrites — changing it requires a redeploy, not just an env update.
API_URL=http://localhost:8080

# Public origin of this Next.js app. In production set to the Vercel URL.
NEXT_PUBLIC_APP_URL=http://localhost:3000
